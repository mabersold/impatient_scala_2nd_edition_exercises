package impatientscala

import chapters._

object main {
  def main(args: Array[String]): Unit = {
    
    
    
//    def countDown(counts: Int) {
//      for (i <- counts to 0 by -1) println(i)
//    }
//    
//    def stringProduct (phrase: String): Long = {
//      if (phrase.length() == 0) 1
//      else stringProduct(phrase.drop(1)) * phrase.head.toLong
//    }
//    
    //Finish this later
    
//    def expo (number: Int, exponent: Int): Int = {
//     
//      if (exponent > 0) {
//        if (exponent % 2 == 0) {
//          
//        }
//      }  
//    }
    
//    val phrase = "Hello"
//    
//    println(stringProduct(phrase)) //should be 9415087488L
    
    val chap01 = new Chapter01
    chap01.runExercises
  }
}